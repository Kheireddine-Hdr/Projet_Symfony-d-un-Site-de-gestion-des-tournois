{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}

    <h1>Création d'un joueur !</h1> 

    {{ form_start(form) }}
    {{ form_row(form.joueurEquipe) }}
    {{ form_row(form.nom, {'attr': {'placeholder': "nom du l'joueur" }}) }}
    {{ form_row(form.prenom, {'attr': {'placeholder': "prenom du joueur"  }}) }}
    {{ form_row(form.poste, {'attr': {'placeholder': "poste du joueur"  }}) }}

        <button type="submit" class="btn btn-success"> Ajouter le joueur </button>

    {{ form_end(form) }}
{% endblock %}