{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}

    <h1>Création d'une equipe !</h1> 

    {{ form_start(form) }}
    {{ form_row(form.groupe) }}
    {{ form_row(form.equipeClub) }}
    {{ form_row(form.nom, {'attr': {'placeholder': "nom du l'equipe" }}) }}
    {{ form_row(form.description, {'attr': {'placeholder': "description du l'equipe"  }}) }}

        <button type="submit" class="btn btn-success"> Ajouter l'equipe </button>

    {{ form_end(form) }}
{% endblock %}